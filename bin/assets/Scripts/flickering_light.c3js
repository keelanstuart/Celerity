var hscl;
var basescl;
var scl;
var runtime = 0.0;
var s;

function update(elapsed_seconds)
{
	runtime += elapsed_seconds;

	s = Math.random(0.9, 1.1);
	scl.x = basescl.x * s;
	scl.y = basescl.y * s;
	scl.z = basescl.z * s;

	SetPropertyValue(hscl, scl);
}

hscl = FindProperty(self, "Scale");
basescl = GetPropertyValue(hscl);
scl = basescl;